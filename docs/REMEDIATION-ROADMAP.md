# نقشهٔ مرحله‌ای اصلاح مشکلات پروژه حسابداری

این سند ترتیب پیشنهادی اصلاحات را مشخص می‌کند. اصل اساسی آن این است که ابتدا قراردادهای مالی و قابلیت بازسازی داده تثبیت شوند، سپس اصلاحات رابط کاربری و قابلیت‌های آینده انجام شود.

> هیچ تغییر داده‌ای روی بکاپ واقعی قبل از اجرای migration آزمایشی، اعتبارسنجی و امکان rollback انجام نشود.

## ترتیب کلی

| مرحله | موضوع | اولویت | نتیجهٔ مورد انتظار |
|---|---|---:|---|
| ۰ | آماده‌سازی و قفل ایمنی | P0 | نسخهٔ قابل بازگشت و fixture مستقل آماده است |
| ۱ | قرارداد قیمت، واحد و پول | P0 | مبلغ همهٔ فاکتورها بدون ابهام محاسبه و نمایش می‌شود |
| ۲ | تقویم جلالی و دیرکرد | P0 | همهٔ بخش‌ها از یک الگوریتم معتبر استفاده می‌کنند |
| ۳ | قیود داده و حذف امن | P0 | ارجاع شکسته و حذف ناخواسته ممکن نیست |
| ۴ | دفتر رویداد موجودی و نقدینگی | P1 | stock و balance قابل بازسازی و حسابرسی می‌شوند |
| ۵ | تولید، چک و گزارش‌ها | P1 | اثر اصلاح، برگشت و جایگزینی قابل ردیابی است |
| ۶ | سخت‌گیری بکاپ، CSV و تست | P1 | انتقال داده و انتشار قابل اعتماد می‌شود |
| ۷ | بهبود رابط کاربری و موبایل | P1 | خطای ورود کمتر و کار با گوشی ساده‌تر می‌شود |
| ۸ | آماده‌سازی APK | P2 | نسخهٔ نصب‌شدنی با migration و offline مطمئن آماده است |
| ۹ | پرتال مشتری و چندکاربره | P2 | دسترسی تفکیک‌شده و امن ایجاد می‌شود |

---

## مرحلهٔ ۰ — آماده‌سازی و قفل ایمنی

### کارها

۱. از آخرین بکاپ معتبر یک نسخهٔ فقط‌خواندنی نگهداری شود و checksum آن ثبت گردد.
۲. یک fixture غیرحساس با کالاهای «عدد/کارتن»، «کیلوگرم/گرم»، چند فاکتور، چند چک، تولید، اصلاح موجودی و عملیات بانکی ساخته شود.
۳. تست‌های موجود از ۲ تست به مجموعهٔ سناریوهای دامنه‌ای منتقل شوند.
۴. اسکریپت اعتبارسنجی بکاپ به تست قابل تکرار تبدیل شود.
۵. قبل از هر migration یک tag یا commit پایدار ایجاد شود.

### آزمون پذیرش

- restore بکاپ روی محیط جدا موفق باشد.
- checksum بکاپ اولیه ثبت شده باشد.
- تست‌ها بدون دادهٔ خصوصی اجرا شوند.
- rollback از نسخهٔ migration شده به نسخهٔ قبل امکان‌پذیر باشد.

### شرط عبور

تا زمانی که بکاپ قابل بازگشت و fixture مستقل وجود ندارد، اصلاحات schema یا تبدیل مبالغ انجام نشود.

---

## مرحلهٔ ۱ — تثبیت قرارداد قیمت، واحد و پول

این مرحله مهم‌ترین اصلاح مالی است.

### تصمیم پیشنهادی

قیمت محصول در مدل اصلی **قیمت هر واحد پایه** باشد. برای نمونه اگر واحد پایه «عدد» و واحد خرید «کارتن» با ضریب ۳۶ باشد، قیمت پایهٔ ۳۰۰٬۰۰۰ ریال/تومان به معنی قیمت هر عدد است و قیمت معادل هر کارتن ۱۰٬۸۰۰٬۰۰۰ خواهد بود.

اگر کسب‌وکار واقعاً قیمت کارتن را وارد می‌کند، باید در فرم مقدار قیمت بر اساس واحد انتخابی وارد شود و تبدیل قیمت به پایه هنگام ثبت انجام گیرد. این دو حالت نباید هم‌زمان و بدون برچسب در یک فیلد مخلوط شوند.

### تغییرات فنی

۱. در مدل محصول نام فیلد به `baseUnit` و `baseUnitPrice` نزدیک شود یا حداقل در UI صریحاً «واحد پایه» نوشته شود.
۲. در فرم فاکتور دو متن روشن نمایش داده شود: «مقدار واردشده» و «معادل واحد پایه».
۳. کنار قیمت، واحد قیمت نمایش داده شود؛ مانند `۳۰۰٬۰۰۰ برای هر عدد`.
۴. قیمت معادل واحد انتخابی به‌صورت preview نشان داده شود.
۵. تاریخچهٔ قیمت شامل `unit`, `price`, `priceBasis` و `effectiveDate` باشد.
۶. تخفیف پس از محاسبهٔ مبلغ پایه اعمال شود و مجموع ریزاقلام با مبلغ نهایی قابل توضیح باشد.
۷. واحد پول در تنظیمات با انتخاب مشخص بین ریال و تومان، ضریب تبدیل و migration روشن مدیریت شود؛ ورود متن آزاد برای واحد پول حذف شود.
۸. مبالغ پولی با integer minor-unit یا decimal دقیق ذخیره شوند و از محاسبهٔ شناور در نقاط حساس جلوگیری شود.

### آزمون پذیرش

- ۱۰ کارتن × قیمت پایهٔ ۳۰۰٬۰۰۰ با ضریب ۳۶، مبلغ ۱۰۸٬۰۰۰٬۰۰۰ تولید کند.
- اگر سیاست قیمت انتخابی فعال شود، همان سناریو فقط با قیمت کارتن ۳۰۰٬۰۰۰، مبلغ ۳٬۰۰۰٬۰۰۰ تولید کند و UI این سیاست را صریح اعلام کند.
- گرم/کیلوگرم، واحد برابر، ضریب اعشاری و ضریب نامعتبر تست شوند.
- ویرایش فاکتور قبلی مبلغ و موجودی را دو بار اعمال نکند.
- backup قدیمی بدون تغییر معنای مبلغ restore شود.

### شرط عبور

تا وقتی تیم پروژه یکی از دو سیاست قیمت را امضا و در `DECISION-LOG.md` ثبت نکرده، فاکتورهای قدیمی نباید bulk-convert شوند.

---

## مرحلهٔ ۲ — یکسان‌سازی تقویم جلالی و دیرکرد

### تغییرات فنی

۱. یک ماژول واحد برای parse، validate، compare و day-difference جلالی ایجاد شود.
۲. date picker، sort، فیلتر تاریخ، FIFO، سود دیرکرد و گزارش‌ها از همان ماژول استفاده کنند.
۳. الگوریتم تقریبی چرخهٔ ۳۳ ساله در مسیرهای متناقض حذف یا به یک کتابخانهٔ معتبر تبدیل شود.
۴. تاریخ نامعتبر، سال صفر، ماه خارج از محدوده و روزهای نامعتبر رد شوند.
۵. مقدار `dayBasis` برای حالت شمسی و حالت ثابت در مدل و گزارش شفاف نمایش داده شود.

### آزمون پذیرش

- پایان اسفند در سال عادی و کبیسه.
- اول فروردین نسبت به آخر اسفند.
- ماه‌های ۳۱ و ۳۰ روزه.
- اختلاف تاریخ برابر، معکوس و تاریخ سررسید قبل از دریافت.
- تطبیق خروجی با چند fixture معتبر مستقل.
- برابر بودن نتیجهٔ date picker و موتور محاسبات.

---

## مرحلهٔ ۳ — قیود داده، حذف امن و اعتبارسنجی بکاپ

### تغییرات فنی

۱. برای کالا، انبار، طرف حساب، حساب، شرط پرداخت، فاکتور و فرمول، تابع مرکزی `findReferences` نوشته شود.
۲. حذف رکورد دارای ارجاع ممنوع شود و فهرست وابستگی‌ها به کاربر نمایش داده شود.
۳. به‌جای حذف واقعی از `archivedAt` یا `active=false` استفاده شود.
۴. تغییر نام یا مشخصات واحد موجود در رکوردهای قبلی ممنوع یا versioned شود.
۵. بکاپ با schema version، migration chain و validation دامنه‌ای بررسی شود.
۶. duplicate number و duplicate code در سطح برنامه کنترل شود.
۷. CSV با RFC 4180، quote، escape، newline و UTF-8 BOM درست تولید شود.

### آزمون پذیرش

- حذف انبار دارای کالا رد شود.
- حذف کالای موجود در فاکتور یا فرمول رد شود.
- حذف چک، transaction خودکار مرتبط را بدون بازسازی نادرست پاک کند.
- بکاپ ناقص یا دارای ID شکسته رد شود.
- import دوبارهٔ export، دادهٔ معادل تولید کند.

---

## مرحلهٔ ۴ — دفتر رویداد موجودی و نقدینگی

### طراحی پیشنهادی موجودی

هر حرکت موجودی یک رکورد append-only داشته باشد:

```text
id
at
date
kind: purchase | sale | production_input | production_output | adjustment | transfer | reversal
productId
warehouseId
quantityEntered
unitEntered
quantityBase
baseUnit
sourceType
sourceId
reversalOf
note
```

### طراحی پیشنهادی نقدینگی

هر اثر پولی یک رکورد مستقل داشته باشد:

```text
id
at
date
kind: receipt | payment | expense | transfer | check_receipt | check_return | reversal
accountId
counterAccountId
amount
currency
sourceType
sourceId
reversalOf
note
```

### تغییرات فنی

۱. stock و balance از روی ledger محاسبه شوند، نه این‌که تنها منبع حقیقت باشند.
۲. ویرایش سند با reversal و رویداد جدید انجام شود؛ مقدار قبلی silently overwrite نشود.
۳. اصلاح دستی موجودی در کاردکس نمایش داده شود.
۴. انتقال بین انبارها دو حرکت متقابل داشته باشد.
۵. بازسازی projection و گزارش مغایرت اضافه شود.
۶. هر رکورد، مقدار ورودی و مقدار پایه را هم‌زمان نگهداری کند.

### آزمون پذیرش

- بازسازی stock با مقدار جاری قبل از migration برابر باشد.
- اصلاح، ابطال و حذف فاکتور فقط یک اثر نهایی داشته باشند.
- تولید مواد را کم و محصول را زیاد کند.
- برگشت چک اثر حساب را معکوس و قابل ردیابی کند.
- گزارش مغایرت برای دادهٔ دستکاری‌شده هشدار بدهد.

---

## مرحلهٔ ۵ — تولید، چک و گزارش‌ها

### تولید

۱. هر تولید یک سند مستقل با مواد واقعی مصرف‌شده، خروجی، هزینه و زمان ثبت کند.
۲. تغییر فرمول جدید روی تولیدهای قبلی اثر نگذارد؛ فرمول در زمان تولید snapshot شود.
۳. اصلاح یا حذف تولید با reversal انجام شود.
۴. بهای مواد بر اساس سیاست مصوب قیمت‌گذاری تعیین شود؛ فعلاً قیمت جاری محصول نباید به‌تنهایی جایگزین بهای واقعی شود.
۵. مرکز هزینه و دورهٔ مالی برای سربار اضافه شود.

### چک و FIFO

۱. وضعیت چک و اثر حساب از هم تفکیک شوند.
۲. چک جایگزین، چک اصلی و آثار مالی آن رابطهٔ قابل بازسازی داشته باشند.
۳. تخصیص FIFO نسخهٔ قابل توضیح داشته باشد: اصل، سود، روز، قاعده و مانده.
۴. تغییر سررسید یا وضعیت چک allocation قبلی را با audit مشخص بازسازی کند.
۵. وصول، برگشت و عودت نباید transaction تکراری بسازند.

### گزارش‌ها

۱. هر عدد گزارش drill-down به سند مادر داشته باشد.
۲. مبلغ اصل، سود، تخفیف، مانده و اثر نقدی جدا نمایش داده شوند.
۳. فیلتر تاریخ از الگوریتم مرحلهٔ ۲ استفاده کند.
۴. گزارش مشتری فقط رکوردهای همان مشتری و همان tenant را برگرداند.

---

## مرحلهٔ ۶ — تست، بکاپ و انتشار

### تغییرات فنی

۱. در `package.json` اسکریپت `test` رسمی اضافه شود.
۲. تست‌های واحد برای مدل، تست integration برای reducer/ledger و تست UI برای فرم‌های حساس ایجاد شود.
۳. تست property-based برای تبدیل واحد و جمع مبلغ اضافه شود.
۴. در CI، check، test، build و migration validation اجرا شوند.
۵. backup قبل و بعد از migration خودکار تولید شود.
۶. checksum و release manifest ثبت شود.
۷. service worker در صورت خطای asset به‌جای برگرداندن HTML نامرتبط، fallback کنترل‌شده داشته باشد.
۸. bundle بزرگ با code splitting برای صفحات گزارش، تولید و پشتیبان تقسیم شود.

### آزمون پذیرش

- CI روی clone تازه موفق شود.
- restore آخرین backup روی clone تازه موفق شود.
- نسخهٔ قدیمی برنامه بتواند backup نسخهٔ پشتیبانی‌شده را بخواند.
- update service worker بدون صفحهٔ سفید انجام شود.

---

## مرحلهٔ ۷ — رابط کاربری و تجربهٔ موبایل

### تغییرات پیشنهادی

۱. فرم فاکتور به بخش‌های «مشخصات»، «اقلام»، «تسویه» و «خلاصه» تقسیم شود.
۲. خلاصهٔ مبلغ و اثر موجودی در موبایل sticky باشد.
۳. برای جدول‌های عریض، نمای کارت یا ستون‌های اولویت‌دار ارائه شود.
۴. عملیات icon-only برچسب قابل مشاهده یا منوی متنی داشته باشند.
۵. خطای هر فیلد کنار همان فیلد نمایش داده شود.
۶. modalها focus را حفظ و پس از بستن به دکمهٔ مبدأ بازگردانند.
۷. selectorهای عمومی CSS با classهای اختصاصی جایگزین شوند.
۸. برای قیمت، مقدار پایه، واحد، سود و وضعیت متن توضیحی کنار رنگ قرار گیرد.
۹. عملیات حذف و اصلاح موجودی در ناحیهٔ خطر جدا شود.

### آزمون پذیرش

- سناریوی ثبت فاکتور با گوشی بدون اسکرول افقی غیرضروری انجام شود.
- screen reader نام دکمه‌های عملیاتی را بخواند.
- خطاهای فرم بدون از دست رفتن ورودی‌ها نمایش داده شوند.
- عرض ۳۲۰، ۳۷۵، ۷۶۸ و دسکتاپ بررسی شود.

---

## مرحلهٔ ۸ — آماده‌سازی APK

۱. migration نسخه‌ها و rollback تعریف شود.
۲. مسیر backup/restore Android آزموده شود.
۳. نصب، update، uninstall/reinstall و offline کامل تست شود.
۴. رفتار localStorage/IndexedDB، quota و پاک‌شدن cache بررسی شود.
۵. ورود و خروج از برنامه و قفل دستگاه برای دادهٔ حساس مشخص شود.
۶. نام، آیکون، manifest و service worker در مسیر واقعی APK بررسی شوند.
۷. release candidate با دادهٔ fixture و سپس با یک کپی کنترل‌شده از دادهٔ واقعی آزموده شود.

APK نباید قبل از عبور مرحلهٔ ۱ تا ۶ منتشر شود.

---

## مرحلهٔ ۹ — پرتال مشتری و چندکاربره

۱. backend و پایگاه دادهٔ مرکزی ایجاد شود.
۲. `tenant_id` به همهٔ جداول مالی اضافه شود.
۳. نقش‌ها شامل مالک، حسابدار و مشتری مشاهده‌گر تعریف شوند.
۴. احراز هویت و session امن ایجاد شود.
۵. Row-Level Security در database فعال شود.
۶. API مشتری فقط گزارش‌ها و رکوردهای مجاز را برگرداند.
۷. بکاپ کامل، تنظیمات، حساب‌های بانکی و مشتریان دیگر هرگز به مشتری ارسال نشود.
۸. audit دسترسی و انقضای session اضافه شود.
۹. sync و conflict resolution بین PWA و backend طراحی شود.

## ترتیب اجرای واقعی

برای شروع، فقط مرحلهٔ ۰ و مرحلهٔ ۱ انجام شود. پس از تأیید قرارداد قیمت/واحد، migration و تست آن، مرحلهٔ ۲ آغاز شود. هر مرحله باید با یک commit جدا و یک ورودی در `DECISION-LOG.md` بسته شود.

### اولین بستهٔ کاری پیشنهادی

- افزودن fixture قیمت پایه و قیمت واحد انتخابی؛
- استخراج تمام نقاط محاسبهٔ مبلغ و تبدیل واحد؛
- تصمیم نهایی دربارهٔ سیاست قیمت؛
- اصلاح برچسب‌ها و مستندات؛
- افزودن تست‌های فاکتور چندواحدی؛
- اجرای دوبارهٔ ممیزی بکاپ؛
- ثبت نتیجه در دفتر تصمیم‌ها.
